# Backtest Reports

This folder stores backtesting reports generated by the system.

## 📊 Report Format

Reports are saved as JSON files with the following naming convention:
```
backtest_report_<SYMBOL>_<TIMESTAMP>.json
```

Example: `backtest_report_IBEX_20251211_143022.json`

## 📋 Report Contents

Each report includes:
- Individual model performance metrics
- Ensemble performance
- Comparison summary
- Date range analyzed
- Confusion matrices
- Accuracy, precision, recall, F1-scores

## 🔍 Viewing Reports

```bash
# View latest report
cat backtest_reports/backtest_report_*.json | jq '.'

# Or use Python
python -c "import json; print(json.dumps(json.load(open('backtest_reports/backtest_report_IBEX_20251211_143022.json')), indent=2))"
```

---

Reports are automatically generated when running backtesting via:
- Dashboard (Tab 3)
- `scripts/quickstart.sh` (Option 3)
- Direct Python call to `backtesting.py`
